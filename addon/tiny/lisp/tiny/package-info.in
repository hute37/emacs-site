(tiny-tools                               ; your package's name
 (standards-version 1.1
  version VERSION
  author-version AUTHOR_VERSION
  date DATE
  build-date BUILD_DATE
  maintainer MAINTAINER
  distribution xemacs              ; change to "mule" if MULE is needed
  priority high
  category CATEGORY
  dump nil
  description "Collection of additional Emacs Utilities"  ; a one-line description string
  filename FILENAME
  md5sum MD5SUM
  size SIZE
  provides (tiny-setup)     ; one for every `provides' form
  requires (REQUIRES)
  type single-file
  ))
